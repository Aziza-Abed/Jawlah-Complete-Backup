{
  "name": "followup-integration-tests",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:smoke": "node quick-smoke-test.js",
    "test:flow": "vitest run --reporter=verbose"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "@microsoft/signalr": "^10.0.0",
    "form-data": "^4.0.0"
  },
  "devDependencies": {
    "vitest": "^3.2.1"
  }
}
