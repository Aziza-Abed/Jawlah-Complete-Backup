# JWT Configuration
# CRITICAL: Generate a strong random secret (minimum 32 characters)
# To generate: Use a password generator or run in PowerShell:
# [System.Convert]::ToBase64String([System.Text.Encoding]::UTF8.GetBytes((1..32 | ForEach-Object { [char](Get-Random -Min 65 -Max 122) }) -join ''))

JwtSettings__SecretKey=YourStrongRandomSecretKeyHere_MinimumThirtyTwoCharacters_ChangeThisNow!

# Database Connection
# For LocalDB (Development):
# CONNECTION_STRING=Server=(localdb)\\FollowUpTest;Database=FollowUp;Trusted_Connection=True;MultipleActiveResultSets=true
# For SQL Server (Production):
# CONNECTION_STRING=Server=your-server;Database=FollowUp;User Id=your-user;Password=your-password;

# IMPORTANT:
# 1. Copy this file to .env (or use Visual Studio user secrets for development)
# 2. NEVER commit .env to git! (.gitignore should exclude it)
# 3. For production: Set JwtSettings__SecretKey as environment variable on server
# 4. ASP.NET Core uses double underscores (__) for nested configuration values
